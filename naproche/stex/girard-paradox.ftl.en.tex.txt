Axiom:
  for all v0, for all v1, for all v2, for all v3, if v0 is an object and v1 is an object and v2 is an object and v3 is an object then (if the ordered pair of v0 and v2 is equal to the ordered pair of v1 and v3 then (v0 is equal to v1 and v2 is equal to v3))

Axiom:
  for all v0, if v0 is a map then (v0 is a function iff the domain of v0 is a set)

Theorem:
  for all v0, for all v1, if v0 is a set and v1 is a class then (if for all v2, if v2 is an element of v1 then v2 is an element of v0 then v1 is a set)
Proof:
  C is a class and (if A is a set then C is a set) and for all v0, v0 is an element of C iff (v0 is an element of A and v0 is an object and v0 is an element of B)
  C is a set and C is equal to B
Qed.

Definition:
  for all v0, for all v1, if v0 is a class and v1 is an object then for all v2, (v2 is equal to \const{ v0 }{ v1 }) iff (v2 is a map and the domain of v2 is equal to v0 and for all v3, if v3 is an element of v0 then the value of v2 under v3 is equal to v1)

Signature:
  for all v0, if v0 is a term then v0 is a set

Axiom:
  for all v0, if v0 is a term then for all v1, if v1 is an element of v0 then v1 is a term

Definition:
  for all v0, if v0 is a term then for all v1, (v1 is a parametrized term over v0) iff (v1 is a function and the domain of v1 is equal to v0 and for all v2, if v2 is an element of v0 then the value of v1 under v2 is a term)

Signature:
  for all v0, if v0 is a term then for all v1, if v1 is a parametrized term over v0 then for all v2, if v2 is equal to \Pi{ v0 }{ v1 } then v2 is a term

Definition:
  for all v0, for all v1, if v0 is a term and v1 is a term then for all v2, (v2 is equal to \Fun{ v0 }{ v1 }) iff v2 is equal to \Pi{ v0 }{ \const{ v0 }{ v1 } }

Signature:
  for all v0, if v0 is a term then for all v1, if v1 is a parametrized term over v0 then for all v2, if v2 is equal to \lambda{ v0 }{ v1 } then v2 is a term

Signature:
  for all v0, if v0 is a sort then v0 is a term

Definition:
  for all v0, for all v1, if v0 is a term and v1 is a term then ((\axiom{ v0 }{ v1 }) iff v0 is an element of v1)

Definition:
  for all v0, for all v1, if v0 is a sort and v1 is a sort then ((\rule{ v0 }{ v1 }) iff for all v2, if v2 is an element of v1 then for all v3, if v3 is a parametrized term over v2 and for all v4, if v4 is an element of v2 then the value of v3 under v4 is an element of v0 then \Pi{ v2 }{ v3 } is an element of v0)

Axiom:
  for all v0, if v0 is a term then for all v1, for all v2, if v1 is a parametrized term over v0 and v2 is a parametrized term over v0 then (if for all v3, if v3 is an element of v0 then the value of v2 under v3 is an element of the value of v1 under v3 then \lambda{ v0 }{ v2 } is an element of \Pi{ v0 }{ v1 })

Axiom:
  for all v0, if v0 is a term then for all v1, for all v2, if v1 is a parametrized term over v0 and v2 is an element of \Pi{ v0 }{ v1 } then (v2 is a parametrized term over v0 and for all v3, if v3 is an element of v0 then the value of v2 under v3 is an element of the value of v1 under v3)

Signature:
  for all v0, if v0 is equal to \starsort then v0 is a sort

Signature:
  for all v0, if v0 is equal to \squaresort then v0 is a sort

Signature:
  for all v0, if v0 is equal to \trianglesort then v0 is a sort

Axiom:
  \axiom{ \starsort }{ \squaresort }

Axiom:
  \axiom{ \squaresort }{ \trianglesort }

Axiom:
  \rule{ \starsort }{ \starsort }

Axiom:
  \rule{ \squaresort }{ \squaresort }

Axiom:
  \rule{ \squaresort }{ \starsort }

Axiom:
  \rule{ \trianglesort }{ \starsort }

Axiom:
  \rule{ \trianglesort }{ \squaresort }

