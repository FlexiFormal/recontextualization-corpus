Signature:
  for all v0, (if v0 is a point then v0 is an object)

Signature (congruence):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Cong{ v0 }{ v1 }{ v2 }{ v3 } then truth holds)))))

Signature (betweenness):
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if \Betw{ v0 }{ v1 }{ v2 } then truth holds))))

Definition (collinearity):
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then ((v0 is collinear with v1 and v2) iff (\Betw{ v0 }{ v1 }{ v2 } or \Betw{ v1 }{ v2 }{ v0 } or \Betw{ v2 }{ v0 }{ v1 })))))

Axiom (reflexivityofcongruence):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then \Cong{ v0 }{ v1 }{ v1 }{ v0 }))

Axiom (pseudotransitivityofcongruence):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Cong{ v2 }{ v3 }{ v0 }{ v1 } and \Cong{ v2 }{ v3 }{ v4 }{ v5 } then \Cong{ v0 }{ v1 }{ v4 }{ v5 })))))))

Axiom (identityofcongruence):
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if \Cong{ v0 }{ v1 }{ v2 }{ v2 } then v0 is equal to v1))))

Axiom (segment_construction):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then for some v4, (v4 is a point and \Betw{ v0 }{ v1 }{ v4 } and \Cong{ v1 }{ v4 }{ v2 }{ v3 })))))

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (for all v6, (for all v7, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point and v6 is a point and v7 is a point then ((\OFS{ v5 }{ v6 }{ v7 }{ v1 }{ v2 }{ v3 }{ v4 }{ v0 }) iff (\Betw{ v5 }{ v6 }{ v7 } and \Betw{ v2 }{ v3 }{ v4 } and (\Cong{ v5 }{ v6 }{ v2 }{ v3 } and \Cong{ v6 }{ v7 }{ v3 }{ v4 } and \Cong{ v5 }{ v1 }{ v2 }{ v0 } and \Cong{ v6 }{ v1 }{ v3 }{ v0 })))))))))))

Axiom (fivesegmentaxiom):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (for all v6, (for all v7, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point and v6 is a point and v7 is a point then (if \OFS{ v0 }{ v2 }{ v4 }{ v6 }{ v1 }{ v3 }{ v5 }{ v7 } and it is wrong that (v0 is equal to v2) then \Cong{ v4 }{ v6 }{ v5 }{ v7 })))))))))

Axiom (identityofbetweenness):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if \Betw{ v0 }{ v1 }{ v0 } then v0 is equal to v1)))

Axiom (innerpasch):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point then (if \Betw{ v2 }{ v0 }{ v4 } and \Betw{ v3 }{ v1 }{ v4 } then for some v5, (v5 is a point and \Betw{ v0 }{ v5 }{ v3 } and \Betw{ v1 }{ v5 }{ v2 })))))))

Axiom (lowerdimension):
  for some v0, (for some v1, (for some v2, (v0 is a point and v1 is a point and v2 is a point and it is wrong that (v0 is collinear with v1 and v2))))

Axiom (upperdimension):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point then (if \Cong{ v2 }{ v0 }{ v2 }{ v1 } and \Cong{ v3 }{ v0 }{ v3 }{ v1 } and \Cong{ v4 }{ v0 }{ v4 }{ v1 } and it is wrong that (v0 is equal to v1) then v2 is collinear with v3 and v4))))))

Axiom (euclid):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v1 is equal to v0) then for all v2, (for all v3, (for all v4, (if v2 is a point and v3 is a point and v4 is a point then (if \Betw{ v1 }{ v0 }{ v2 } and \Betw{ v3 }{ v0 }{ v4 } then for some v5, (for some v6, (v5 is a point and v6 is a point and (\Betw{ v1 }{ v3 }{ v5 } and \Betw{ v1 }{ v4 }{ v6 } and \Betw{ v5 }{ v2 }{ v6 }))))))))))

Axiom (circlecontinuity):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v3 }{ v0 }{ v1 } and \Betw{ v3 }{ v1 }{ v2 } then for all v4, (for all v5, (if v4 is a point and v5 is a point then (if \Cong{ v3 }{ v4 }{ v3 }{ v0 } and \Cong{ v3 }{ v5 }{ v3 }{ v2 } then for some v6, (v6 is a point and \Cong{ v3 }{ v6 }{ v3 }{ v1 } and \Betw{ v4 }{ v6 }{ v5 })))))))))

Theorem (reflexivityofcongruence):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then \Cong{ v0 }{ v1 }{ v0 }{ v1 }))
Proof:
Qed.

Theorem (symmetryofcongruence):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Cong{ v2 }{ v3 }{ v0 }{ v1 } then \Cong{ v0 }{ v1 }{ v2 }{ v3 })))))
Proof:
Qed.

Theorem (transitivityofcongruence):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Cong{ v4 }{ v5 }{ v2 }{ v3 } and \Cong{ v2 }{ v3 }{ v0 }{ v1 } then \Cong{ v4 }{ v5 }{ v0 }{ v1 })))))))
Proof:
Qed.

Theorem (congruenceisindependentoftheorderofthepairs):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Cong{ v2 }{ v3 }{ v0 }{ v1 } then \Cong{ v3 }{ v2 }{ v0 }{ v1 })))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Cong{ v2 }{ v3 }{ v0 }{ v1 } then \Cong{ v2 }{ v3 }{ v1 }{ v0 })))))
Proof:
Qed.

Theorem (zerosegmentsarecongruent):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then \Cong{ v0 }{ v0 }{ v1 }{ v1 }))
Proof:
Qed.

Theorem (concatenationofsegments):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Betw{ v3 }{ v4 }{ v5 } and \Betw{ v0 }{ v1 }{ v2 } then (if \Cong{ v3 }{ v4 }{ v0 }{ v1 } and \Cong{ v4 }{ v5 }{ v1 }{ v2 } then \Cong{ v3 }{ v5 }{ v0 }{ v2 }))))))))
Proof:
  \OFS{ x }{ y }{ z }{ x }{ r }{ v }{ w }{ r }
  if x is equal to y then r is equal to v
  if it is wrong that (x is equal to y) then \Cong{ x }{ z }{ r }{ w }
Qed.

Theorem (uniquenessofsegmentconstruction):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then for all v2, (for all v3, (for all v4, (if v2 is a point and v3 is a point and v4 is a point then (if \Betw{ v0 }{ v1 }{ v2 } and \Cong{ v1 }{ v2 }{ v3 }{ v4 } then for all v5, (if v5 is a point then (if \Betw{ v0 }{ v1 }{ v5 } and \Cong{ v1 }{ v5 }{ v3 }{ v4 } then v2 is equal to v5)))))))))
Proof:
  \Cong{ a }{ c }{ a }{ c' }
  \Cong{ b }{ c }{ b }{ c' }
  \OFS{ a }{ b }{ c }{ c }{ a }{ b }{ c }{ c' }
  \Cong{ c }{ c }{ c }{ c' }
Qed.

Theorem (rightbetweenness):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then \Betw{ v0 }{ v1 }{ v1 }))
Proof:
Qed.

Theorem (symmetryofbetweenness):
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if \Betw{ v0 }{ v1 }{ v2 } then \Betw{ v2 }{ v1 }{ v0 }))))
Proof:
Qed.

Theorem (leftbetweenness):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then \Betw{ v0 }{ v0 }{ v1 }))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if \Betw{ v0 }{ v1 }{ v2 } and \Betw{ v1 }{ v0 }{ v2 } then v0 is equal to v1))))
Proof:
  w is a point and \Betw{ y }{ w }{ y } and \Betw{ x }{ w }{ x }
  x is equal to w and w is equal to y
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v1 }{ v2 }{ v0 } and \Betw{ v2 }{ v3 }{ v0 } then \Betw{ v1 }{ v2 }{ v3 })))))
Proof:
  w is a point and \Betw{ y }{ w }{ y } and \Betw{ z }{ w }{ x }
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v1 }{ v2 }{ v3 } and \Betw{ v2 }{ v3 }{ v0 } and it is wrong that (v2 is equal to v3) then \Betw{ v1 }{ v3 }{ v0 })))))
Proof:
  v is a point and \Betw{ x }{ z }{ v } and \Cong{ z }{ v }{ z }{ r }
  \Betw{ y }{ z }{ v } and \Cong{ z }{ v }{ z }{ r }
  v is equal to r
Qed.

Theorem (s35b):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v1 }{ v2 }{ v0 } and \Betw{ v2 }{ v3 }{ v0 } then \Betw{ v1 }{ v3 }{ v0 })))))
Proof:
  if y is equal to z then \Betw{ x }{ z }{ v }
  it is wrong that (y is equal to z)
  \Betw{ x }{ y }{ z }
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v1 }{ v2 }{ v3 } and \Betw{ v1 }{ v3 }{ v0 } then \Betw{ v2 }{ v3 }{ v0 })))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v1 }{ v2 }{ v3 } and \Betw{ v1 }{ v3 }{ v0 } then \Betw{ v1 }{ v2 }{ v0 })))))
Proof:
  \Betw{ r }{ z }{ x }
  \Betw{ z }{ y }{ x }
  \Betw{ r }{ y }{ x }
  \Betw{ x }{ y }{ z }
Qed.

Theorem:
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then for all v2, (for all v3, (if v2 is a point and v3 is a point then (if \Betw{ v3 }{ v0 }{ v1 } and \Betw{ v0 }{ v1 }{ v2 } then \Betw{ v3 }{ v0 }{ v2 }))))))
Proof:
Qed.

Theorem:
  for some v0, (for some v1, (v0 is a point and v1 is a point and it is wrong that (v0 is equal to v1)))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (if v0 is a point and v1 is a point then for some v2, (v2 is a point and \Betw{ v0 }{ v1 }{ v2 } and it is wrong that (v1 is equal to v2))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Betw{ v3 }{ v4 }{ v5 } and \Betw{ v1 }{ v2 }{ v5 } and \Betw{ v3 }{ v0 }{ v1 } then for some v6, (v6 is a point and \Betw{ v0 }{ v6 }{ v5 } and \Betw{ v4 }{ v6 }{ v2 }))))))))
Proof:
  \Betw{ x }{ p }{ u } and \Betw{ z }{ v }{ u }
  r is a point and \Betw{ v }{ r }{ x } and \Betw{ p }{ r }{ z }
  SetBytes prover "vampire"  q is a point and \Betw{ r }{ q }{ z } and \Betw{ v }{ q }{ y }
Qed.

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (for all v6, (for all v7, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point and v6 is a point and v7 is a point then ((\IFS{ v5 }{ v6 }{ v7 }{ v2 }{ v3 }{ v4 }{ v0 }{ v1 }) iff (\Betw{ v5 }{ v6 }{ v7 } and \Betw{ v3 }{ v4 }{ v0 } and \Cong{ v5 }{ v7 }{ v3 }{ v0 } and \Cong{ v6 }{ v7 }{ v4 }{ v0 } and \Cong{ v5 }{ v2 }{ v3 }{ v1 } and \Cong{ v7 }{ v2 }{ v0 }{ v1 }))))))))))

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (for all v6, (for all v7, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point and v6 is a point and v7 is a point then (if \IFS{ v5 }{ v6 }{ v7 }{ v2 }{ v3 }{ v4 }{ v0 }{ v1 } then \Cong{ v6 }{ v2 }{ v4 }{ v1 })))))))))
Proof:
  if it is wrong that (x is equal to z) then the thesis holds
  Proof:
    g is a point and h is a point and (it is wrong that (g is equal to z) and \Betw{ x }{ z }{ g } and \Betw{ v }{ p }{ h } and \Cong{ p }{ h }{ z }{ g })
    \OFS{ x }{ z }{ g }{ r }{ v }{ p }{ h }{ q }
    \Cong{ g }{ r }{ h }{ q }
    \OFS{ g }{ z }{ y }{ r }{ h }{ p }{ w }{ q }
    \Cong{ y }{ r }{ w }{ q }
  Qed.
Qed.

Theorem (overlappingsegments):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Betw{ v3 }{ v4 }{ v5 } and \Betw{ v0 }{ v1 }{ v2 } and \Cong{ v3 }{ v5 }{ v0 }{ v2 } and \Cong{ v4 }{ v5 }{ v1 }{ v2 } then \Cong{ v3 }{ v4 }{ v0 }{ v1 })))))))
Proof:
  \IFS{ x }{ y }{ z }{ x }{ r }{ v }{ w }{ r }
Qed.

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then ((\SenaryCong{ v3 }{ v4 }{ v5 }{ v0 }{ v1 }{ v2 }) iff (\Cong{ v3 }{ v4 }{ v0 }{ v1 } and \Cong{ v3 }{ v5 }{ v0 }{ v2 } and \Cong{ v4 }{ v5 }{ v1 }{ v2 }))))))))

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point then (if \Betw{ v2 }{ v3 }{ v4 } and \Cong{ v2 }{ v4 }{ v0 }{ v1 } then for some v5, (v5 is a point and \Betw{ v0 }{ v5 }{ v1 } and \SenaryCong{ v2 }{ v3 }{ v4 }{ v0 }{ v5 }{ v1 })))))))
Proof:
  u is a point and \Betw{ w }{ r }{ u } and it is wrong that (r is equal to u)
  v is a point and \Betw{ u }{ r }{ v } and \Cong{ r }{ v }{ x }{ y }
  g is a point and \Betw{ u }{ v }{ g } and \Cong{ v }{ g }{ y }{ z }
  \Cong{ x }{ z }{ r }{ w }
  g is equal to w
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Betw{ v3 }{ v4 }{ v5 } and \SenaryCong{ v3 }{ v4 }{ v5 }{ v0 }{ v1 }{ v2 } then \Betw{ v0 }{ v1 }{ v2 })))))))
Proof:
  u is a point and \Betw{ r }{ u }{ w } and \SenaryCong{ x }{ y }{ z }{ r }{ u }{ w }
  \SenaryCong{ r }{ u }{ w }{ r }{ v }{ w } and \IFS{ r }{ u }{ w }{ u }{ r }{ u }{ w }{ v }
  \Cong{ u }{ u }{ u }{ v }
  u is equal to v
  \Betw{ r }{ v }{ w }
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 is collinear with v1 and v2 then v1 is collinear with v2 and v0))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 is collinear with v1 and v2 then v2 is collinear with v0 and v1))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 is collinear with v1 and v2 then v2 is collinear with v1 and v0))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 is collinear with v1 and v2 then v1 is collinear with v0 and v2))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 is collinear with v1 and v2 then v0 is collinear with v2 and v1))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (if v0 is a point and v1 is a point then v0 is collinear with v0 and v1))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 is collinear with v1 and v2 then for all v3, (for all v4, (if v3 is a point and v4 is a point then (if \Cong{ v0 }{ v1 }{ v3 }{ v4 } then for some v5, (v5 is a point and \SenaryCong{ v0 }{ v1 }{ v2 }{ v3 }{ v4 }{ v5 }))))))))
Proof:
  if \Betw{ a }{ b }{ c } then the thesis holds
  Proof:
    c' is a point and \Betw{ a' }{ b' }{ c' } and \Cong{ b' }{ c' }{ b }{ c }
  Qed.
  if \Betw{ b }{ a }{ c } then the thesis holds
  Proof:
    c' is a point and \Betw{ b' }{ a' }{ c' } and \Cong{ a' }{ c' }{ a }{ c }
    \Cong{ b }{ c }{ b' }{ c' }
  Qed.
  \Betw{ a }{ c }{ b }
  c' is a point and \Betw{ a' }{ c' }{ b' } and \SenaryCong{ a }{ c }{ b }{ a' }{ c' }{ b' }
Qed.

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (for all v6, (for all v7, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point and v6 is a point and v7 is a point then ((\FS{ v5 }{ v6 }{ v7 }{ v2 }{ v3 }{ v4 }{ v0 }{ v1 }) iff (v5 is collinear with v6 and v7 and \SenaryCong{ v5 }{ v6 }{ v7 }{ v3 }{ v4 }{ v0 } and \Cong{ v5 }{ v2 }{ v3 }{ v1 } and \Cong{ v6 }{ v2 }{ v4 }{ v1 }))))))))))

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (for all v6, (for all v7, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point and v6 is a point and v7 is a point then (if \FS{ v5 }{ v6 }{ v7 }{ v2 }{ v3 }{ v4 }{ v0 }{ v1 } and it is wrong that (v5 is equal to v6) then \Cong{ v7 }{ v2 }{ v0 }{ v1 })))))))))
Proof:
  if \Betw{ x }{ y }{ z } then the thesis holds
  Proof:
    \SenaryCong{ x }{ y }{ z }{ v }{ w }{ p }
    \Betw{ v }{ w }{ p }
    \OFS{ x }{ y }{ z }{ r }{ v }{ w }{ p }{ q }
  Qed.
  if \Betw{ z }{ x }{ y } then the thesis holds
  Proof:
    \SenaryCong{ z }{ x }{ y }{ p }{ v }{ w }
    \Betw{ p }{ v }{ w }
    \OFS{ y }{ x }{ z }{ r }{ w }{ v }{ p }{ q }
  Qed.
  \Betw{ y }{ z }{ x }
  \SenaryCong{ y }{ z }{ x }{ w }{ p }{ v }
  \Betw{ w }{ p }{ v }
  \IFS{ y }{ z }{ x }{ r }{ w }{ p }{ v }{ q }
Qed.

Theorem:
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then for all v2, (if v2 is a point then (if v0 is collinear with v1 and v2 then for all v3, (for all v4, (if v3 is a point and v4 is a point then (if \Cong{ v0 }{ v3 }{ v0 }{ v4 } and \Cong{ v1 }{ v3 }{ v1 }{ v4 } then \Cong{ v2 }{ v3 }{ v2 }{ v4 }))))))))
Proof:
  \FS{ x }{ y }{ z }{ p }{ x }{ y }{ z }{ q }
Qed.

Theorem:
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then for all v2, (if v2 is a point then (if v0 is collinear with v1 and v2 then for all v3, (if v3 is a point then (if \Cong{ v0 }{ v2 }{ v0 }{ v3 } and \Cong{ v1 }{ v2 }{ v1 }{ v3 } then v3 is equal to v2)))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v1 }{ v3 }{ v2 } and \Cong{ v1 }{ v3 }{ v1 }{ v0 } and \Cong{ v2 }{ v3 }{ v2 }{ v0 } then v3 is equal to v0)))))
Proof:
  x is equal to y
  x is equal to z and x is equal to p
  z is equal to p
  it is wrong that (x is equal to y)
Qed.

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then ((\BetwFour{ v0 }{ v1 }{ v2 }{ v3 }) iff (\Betw{ v0 }{ v1 }{ v2 } and \Betw{ v0 }{ v1 }{ v3 } and \Betw{ v0 }{ v2 }{ v3 } and \Betw{ v1 }{ v2 }{ v3 }))))))

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point then ((\BetwFive{ v0 }{ v1 }{ v2 }{ v3 }{ v4 }) iff (\Betw{ v0 }{ v1 }{ v2 } and \Betw{ v0 }{ v1 }{ v3 } and \Betw{ v0 }{ v1 }{ v4 } and \Betw{ v0 }{ v2 }{ v3 } and \Betw{ v0 }{ v2 }{ v4 } and \Betw{ v0 }{ v3 }{ v4 } and \Betw{ v1 }{ v2 }{ v3 } and \Betw{ v1 }{ v2 }{ v4 } and \Betw{ v1 }{ v3 }{ v4 } and \Betw{ v2 }{ v3 }{ v4 })))))))

Theorem (extension_to_quaternary_betweenness):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v0 }{ v1 }{ v2 } and \Betw{ v0 }{ v2 }{ v3 } then \BetwFour{ v0 }{ v1 }{ v2 }{ v3 })))))
Proof:
Qed.

Theorem (extensiontoquinarybetweenness):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point then (if \BetwFour{ v0 }{ v1 }{ v2 }{ v3 } and \Betw{ v0 }{ v3 }{ v4 } then \BetwFive{ v0 }{ v1 }{ v2 }{ v3 }{ v4 }))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if it is wrong that (v1 is equal to v2) and \Betw{ v1 }{ v2 }{ v3 } and \Betw{ v1 }{ v2 }{ v0 } then for some v4, (for some v5, (v4 is a point and v5 is a point and (\Betw{ v1 }{ v0 }{ v4 } and \Cong{ v0 }{ v4 }{ v3 }{ v0 } and \Betw{ v1 }{ v3 }{ v5 } and \Cong{ v3 }{ v5 }{ v3 }{ v0 }))))))))
Proof:
  a is a point and \Betw{ x }{ r }{ a } and \Cong{ r }{ a }{ z }{ r }
  b is a point and \Betw{ x }{ z }{ b } and \Cong{ z }{ b }{ z }{ r }
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if it is wrong that (v3 is equal to v4) and \Betw{ v3 }{ v4 }{ v5 } and \Betw{ v3 }{ v4 }{ v2 } and \Betw{ v3 }{ v2 }{ v0 } and \Cong{ v2 }{ v0 }{ v5 }{ v2 } and \Betw{ v3 }{ v5 }{ v1 } and \Cong{ v5 }{ v1 }{ v5 }{ v2 } then for some v6, (for some v7, (v6 is a point and v7 is a point and (\Betw{ v5 }{ v1 }{ v7 } and \Betw{ v2 }{ v0 }{ v6 }))))))))))
Proof:
Qed.

Theorem (outerpasch):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then for all v2, (for all v3, (if v2 is a point and v3 is a point then (if \Betw{ v0 }{ v1 }{ v2 } and \Betw{ v0 }{ v1 }{ v3 } then (\Betw{ v0 }{ v2 }{ v3 } or \Betw{ v0 }{ v3 }{ v2 })))))))
Proof:
  c' is a point and \Betw{ a }{ d }{ c' } and \Cong{ d }{ c' }{ c }{ d }
  d' is a point and \Betw{ a }{ c }{ d' } and \Cong{ c }{ d' }{ c }{ d }
  c is equal to c' or d is equal to d'
  Proof:
    \BetwFour{ a }{ b }{ c }{ d' }
    \BetwFour{ a }{ b }{ d }{ c' }
    b' is a point and \Betw{ a }{ c' }{ b' } and \Cong{ c' }{ b' }{ c }{ b }
    b'' is a point and \Betw{ a }{ d' }{ b'' } and \Cong{ d' }{ b'' }{ b }{ d }
    \BetwFive{ a }{ b }{ c }{ d' }{ b'' }
    \BetwFive{ a }{ b }{ d }{ c' }{ b' }
    SetInt timelimit 90    \Cong{ b }{ c' }{ b'' }{ c }
    \Cong{ b }{ b' }{ b'' }{ b }
    \Betw{ a }{ b }{ b' } and \Betw{ a }{ b }{ b'' }
    b'' is equal to b'
    \OFS{ b }{ c }{ d' }{ c' }{ b' }{ c' }{ d }{ c }
    \Cong{ c' }{ d' }{ c }{ d }
    SetInt timelimit 65    e is a point and \Betw{ c }{ e }{ c' } and \Betw{ d }{ e }{ d' }
    \IFS{ d }{ e }{ d' }{ c }{ d }{ e }{ d' }{ c' } and \IFS{ c }{ e }{ c' }{ d }{ c }{ e }{ c' }{ d' }
    \Cong{ e }{ c }{ e }{ c' } and \Cong{ e }{ d }{ e }{ d' }
    if it is wrong that (c is equal to c') then the thesis holds
    Proof:
      it is wrong that (c is equal to d')
      p is a point and \Betw{ c' }{ c }{ p } and \Cong{ c }{ p }{ c }{ d' }
      r is a point and \Betw{ d' }{ c }{ r } and \Cong{ c }{ r }{ c }{ e }
      q is a point and \Betw{ p }{ r }{ q } and \Cong{ r }{ q }{ r }{ p }
      \OFS{ d' }{ c }{ r }{ p }{ p }{ c }{ e }{ d' }
      \Cong{ r }{ p }{ e }{ d' }
      \Cong{ r }{ q }{ e }{ d }
      \OFS{ d' }{ e }{ d }{ c }{ p }{ r }{ q }{ c }
      \Cong{ d' }{ d }{ p }{ q }
      \Cong{ c }{ q }{ c }{ d }
      \Cong{ c }{ p }{ c }{ q }
      \Cong{ r }{ p }{ r }{ q }
      it is wrong that (r is equal to c)
      r is collinear with c and d'
      \Cong{ d' }{ p }{ d' }{ q }
      it is wrong that (c is equal to d')
      c is collinear with d' and b
      c is collinear with d' and b'
      \Cong{ b }{ p }{ b }{ q } and \Cong{ b' }{ p }{ b' }{ q }
      it is wrong that (b is equal to b')
      b is collinear with c' and b'
      \Cong{ c' }{ p }{ c' }{ q }
      c' is collinear with c and p
      \Cong{ p }{ p }{ p }{ q }
      p is equal to q
      d is equal to d'
    Qed.
  Qed.
  \Betw{ a }{ c }{ d } or \Betw{ a }{ d }{ c }
Qed.

Theorem:
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then for all v2, (for all v3, (if v2 is a point and v3 is a point then (if \Betw{ v0 }{ v1 }{ v2 } and \Betw{ v0 }{ v1 }{ v3 } then (\Betw{ v1 }{ v2 }{ v3 } or \Betw{ v1 }{ v3 }{ v2 })))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Betw{ v1 }{ v2 }{ v0 } and \Betw{ v1 }{ v3 }{ v0 } then (\Betw{ v1 }{ v2 }{ v3 } or \Betw{ v1 }{ v3 }{ v2 }))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then for all v2, (for all v3, (if v2 is a point and v3 is a point then for some v4, (v4 is a point and (\Betw{ v0 }{ v1 }{ v4 } or \Betw{ v0 }{ v4 }{ v1 }) and \Cong{ v0 }{ v4 }{ v2 }{ v3 }))))))
Proof:
  b' is a point and \Betw{ b }{ a }{ b' } and \Cong{ a }{ b' }{ a }{ b }
  x is a point and \Betw{ b' }{ a }{ x } and \Cong{ a }{ x }{ c }{ d }
Qed.

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then ((\Leq{ v0 }{ v1 }{ v2 }{ v3 }) iff for some v4, (v4 is a point and \Betw{ v2 }{ v4 }{ v3 } and \Cong{ v0 }{ v1 }{ v2 }{ v4 }))))))

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Leq{ v0 }{ v1 }{ v2 }{ v3 } then for some v4, (v4 is a point and \Betw{ v0 }{ v1 }{ v4 } and \Cong{ v0 }{ v4 }{ v2 }{ v3 }))))))
Proof:
  y is a point and \Betw{ c }{ y }{ d } and \Cong{ a }{ b }{ c }{ y }
  x is a point and \Betw{ a }{ b }{ x } and \Cong{ b }{ x }{ y }{ d }
  \Cong{ a }{ x }{ c }{ d }
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then for all v4, (if v4 is a point and \Betw{ v0 }{ v1 }{ v4 } and \Cong{ v0 }{ v4 }{ v2 }{ v3 } then \Leq{ v0 }{ v1 }{ v2 }{ v3 })))))
Proof:
  b' is a point and \Betw{ c }{ b' }{ d } and \SenaryCong{ a }{ b }{ x }{ c }{ b' }{ d }
Qed.

Theorem (transitivityofcongruenceandcomparison):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Cong{ v0 }{ v1 }{ v2 }{ v3 } and \Leq{ v2 }{ v3 }{ v4 }{ v5 } then \Leq{ v0 }{ v1 }{ v4 }{ v5 })))))))
Proof:
  x is a point and \Betw{ e }{ x }{ f } and \Cong{ c }{ d }{ e }{ x }
Qed.

Theorem (transitivityofcomparisonandcongruence):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Leq{ v0 }{ v1 }{ v2 }{ v3 } and \Cong{ v2 }{ v3 }{ v4 }{ v5 } then \Leq{ v0 }{ v1 }{ v4 }{ v5 })))))))
Proof:
  x is a point and \Betw{ a }{ b }{ x } and \Cong{ a }{ x }{ c }{ d }
Qed.

Theorem (reflexivityofcomparison):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then \Leq{ v0 }{ v1 }{ v0 }{ v1 }))
Proof:
Qed.

Theorem (transitivityofcomparison):
  for all v0, (for all v1, (for all v2, (for all v3, (for all v4, (for all v5, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point and v4 is a point and v5 is a point then (if \Leq{ v0 }{ v1 }{ v2 }{ v3 } and \Leq{ v2 }{ v3 }{ v4 }{ v5 } then \Leq{ v0 }{ v1 }{ v2 }{ v3 })))))))
Proof:
  x is a point and \Betw{ a }{ b }{ x } and \Cong{ a }{ x }{ c }{ d }
  y is a point and \Betw{ c }{ d }{ y } and \Cong{ c }{ y }{ e }{ f }
Qed.

Theorem (antisymmetryofcomparison):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if \Leq{ v0 }{ v1 }{ v2 }{ v3 } and \Leq{ v2 }{ v3 }{ v0 }{ v1 } then \Cong{ v0 }{ v1 }{ v2 }{ v3 })))))
Proof:
  x is a point and \Betw{ c }{ x }{ d } and \Cong{ a }{ b }{ c }{ x }
  y is a point and \Betw{ c }{ d }{ y } and \Cong{ a }{ b }{ c }{ y }
  \Cong{ c }{ x }{ c }{ y }
  x is equal to d and d is equal to y
Qed.

Theorem (connexityofcomparison):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (\Leq{ v0 }{ v1 }{ v2 }{ v3 } or \Leq{ v2 }{ v3 }{ v0 }{ v1 })))))
Proof:
  if it is wrong that (a is equal to b) then the thesis holds
  Proof:
    x is a point and (\Betw{ b }{ a }{ x } or \Betw{ b }{ x }{ a }) and \Cong{ b }{ x }{ c }{ d }
  Qed.
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then \Leq{ v0 }{ v0 }{ v1 }{ v2 })))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if \Betw{ v0 }{ v1 }{ v2 } then \Leq{ v0 }{ v1 }{ v0 }{ v2 }))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if \Betw{ v0 }{ v1 }{ v2 } then \Leq{ v1 }{ v2 }{ v0 }{ v2 }))))
Proof:
  a is a point and \Betw{ c }{ b }{ a } and \Cong{ c }{ a }{ a }{ c }
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 is collinear with v1 and v2 then (if \Leq{ v0 }{ v1 }{ v0 }{ v2 } and \Leq{ v1 }{ v2 }{ v0 }{ v2 } then \Betw{ v0 }{ v1 }{ v2 })))))
Proof:
Qed.

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then ((\Less{ v0 }{ v1 }{ v2 }{ v3 }) iff (\Leq{ v0 }{ v1 }{ v2 }{ v3 } and it is wrong that (\Cong{ v0 }{ v1 }{ v2 }{ v3 })))))))

Definition:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then ((\Greater{ v0 }{ v1 }{ v2 }{ v3 }) iff \Less{ v2 }{ v3 }{ v0 }{ v1 })))))

Definition:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then ((v0 and v1 lie on opposite sides of v2) iff (it is wrong that (v0 is equal to v1) and it is wrong that (v0 is equal to v2) and it is wrong that (v1 is equal to v2) and \Betw{ v0 }{ v2 }{ v1 })))))

Definition:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then ((v0 and v1 are equivalent with respect to v2) iff (it is wrong that (v0 is equal to v2) and it is wrong that (v1 is equal to v2) and (\Betw{ v2 }{ v0 }{ v1 } or \Betw{ v2 }{ v1 }{ v0 }))))))

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if it is wrong that (v0 is equal to v3) and it is wrong that (v1 is equal to v3) and it is wrong that (v2 is equal to v3) then (if \Betw{ v0 }{ v3 }{ v2 } then (\Betw{ v1 }{ v3 }{ v2 } iff v0 and v1 are equivalent with respect to v3)))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 and v1 are equivalent with respect to v2 then (it is wrong that (v0 is equal to v2) and it is wrong that (v1 is equal to v2) and for some v3, (v3 is a point and it is wrong that (v3 is equal to v2) and \Betw{ v0 }{ v2 }{ v3 } and \Betw{ v1 }{ v2 }{ v3 }))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if it is wrong that (v0 is equal to v2) and it is wrong that (v1 is equal to v2) then (if for some v3, (v3 is a point and it is wrong that (v3 is equal to v2) and \Betw{ v0 }{ v2 }{ v3 } and \Betw{ v1 }{ v2 }{ v3 }) then v0 and v1 are equivalent with respect to v2)))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (v0 and v1 are equivalent with respect to v2 iff (v0 is collinear with v2 and v1 and it is wrong that (\Betw{ v0 }{ v2 }{ v1 }))))))
Proof:
Qed.

Theorem (reflexivityofrelativeequivalence):
  for all v0, (for all v1, (if v0 is a point and v1 is a point then (if it is wrong that (v0 is equal to v1) then v0 and v0 are equivalent with respect to v1)))
Proof:
Qed.

Theorem (symmetryofrelativeequivalence):
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 and v1 are equivalent with respect to v2 then v1 and v0 are equivalent with respect to v2))))
Proof:
Qed.

Theorem (transitivityofrelativeequivalence):
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if v0 and v1 are equivalent with respect to v3 and v1 and v2 are equivalent with respect to v3 then v0 and v2 are equivalent with respect to v3)))))
Proof:
  if \Betw{ u }{ a }{ b } then the thesis holds
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if it is wrong that (v3 is equal to v0) and it is wrong that (v1 is equal to v2) then for some v4, (v4 is a point and v4 and v3 are equivalent with respect to v0 and \Cong{ v0 }{ v4 }{ v1 }{ v2 }))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (for all v3, (if v0 is a point and v1 is a point and v2 is a point and v3 is a point then (if it is wrong that (v3 is equal to v0) and it is wrong that (v1 is equal to v2) then for all v4, (if v4 is a point and v4 and v3 are equivalent with respect to v0 and \Cong{ v0 }{ v4 }{ v1 }{ v2 } then for all v5, (if v5 is a point and v5 and v3 are equivalent with respect to v0 and \Cong{ v0 }{ v5 }{ v1 }{ v2 } then v5 is equal to v4)))))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 and v1 are equivalent with respect to v2 and \Leq{ v2 }{ v0 }{ v2 }{ v1 } then \Betw{ v2 }{ v0 }{ v1 }))))
Proof:
Qed.

Theorem:
  for all v0, (for all v1, (for all v2, (if v0 is a point and v1 is a point and v2 is a point then (if v0 and v1 are equivalent with respect to v2 and \Betw{ v2 }{ v0 }{ v1 } then \Leq{ v2 }{ v0 }{ v2 }{ v1 }))))
Proof:
Qed.

